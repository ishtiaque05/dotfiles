{{- if eq .chezmoi.os "darwin" }}
# Brewfile for macOS
# Install with: brew bundle install
# Check status: brew bundle check
# Cleanup unused: brew bundle cleanup

# Taps (third-party repositories)
tap "wez/wezterm"

# Core tools
brew "git"
brew "curl"
brew "zsh"
brew "neovim"     # Modern vim-based editor
brew "tmux"       # Terminal multiplexer

# Modern CLI tools
brew "eza"        # Modern ls replacement
brew "bat"        # Modern cat replacement
brew "fzf"        # Fuzzy finder
brew "fd"         # Modern find replacement (feeds fzf + Telescope)
brew "zoxide"     # Smart cd replacement
brew "tldr"       # Simplified man pages
brew "lazygit"    # Terminal git UI
brew "git-delta"  # Syntax-highlighted git diffs

# Applications
cask "wezterm"    # Terminal emulator
{{- end }}
